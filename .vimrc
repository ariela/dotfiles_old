" vim: set ts=4 sw=4 sts=0:

let $VMBUNDLE_PATH = '/home/yuki/.vim'
let $VMCONFIG_PATH = '/home/yuki/dotfiles/vim.d'

source $VMCONFIG_PATH/functions/common

source $VMCONFIG_PATH/bundles
source $VMCONFIG_PATH/colors
source $VMCONFIG_PATH/statusline

source $VMCONFIG_PATH/basic
source $VMCONFIG_PATH/display
source $VMCONFIG_PATH/indent
source $VMCONFIG_PATH/histroy
source $VMCONFIG_PATH/search
source $VMCONFIG_PATH/encode
source $VMCONFIG_PATH/keybind
